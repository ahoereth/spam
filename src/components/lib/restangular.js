import {
  includes, isUndefined, isNull, defaults, isArray, each, extend, find, has,
  initial, last, reduce, isBoolean, keys, isEmpty, bind, union, values, pick,
  isNumber, omit, map, every, isObject, isFunction
} from 'lodash-es';
import 'restangular';

window._ = {
  includes, isUndefined, isNull, defaults, isArray, each, extend, find, has,
  initial, last, reduce, isBoolean, keys, isEmpty, bind, union, values, pick,
  isNumber, omit, map, every, isObject, isFunction
};

export default 'restangular';
